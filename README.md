# Using LLM to play Pokemon (wake me up when this works)

.env should contain
```
OPENAI_API_KEY=sk-****

# for local model
BASE_URL=http://localhost:1234/v1
OPENAI_API_KEY=lm-studio
```

Then `python main.py` to run